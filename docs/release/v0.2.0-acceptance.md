# Ailuros v0.2.0 Release Acceptance

Pack: 0084.v020-evidence-pipeline-acceptance
Date: 2026-06-05

## Release status

Status: accepted

## Capability claimed

v0.2.0 delivers **evidence-only capability**: structured `EvidenceRecord` ingestion,
timeline storage, JSON/JSONL export, evidence-based evaluation, and regression
testing. Evidence enters the runtime from reference-application fixtures; no
runtime control flows back to external applications.

## Acceptance Matrix

| Component | Location | Status | Evidence |
|---|---|---|---|
| EvidenceRecord model | `src/ailuros/models/evidence.py` | **IMPLEMENTED** | 5 fields (`version`, `run_id`, `event_type`, `payload`, `timestamp`), `extra="forbid"`, opaque payload, tz-aware validation |
| Evidence ingest | `src/ailuros/evidence/ingest.py` | **IMPLEMENTED** | `ingest_evidence()` stores records as `EVIDENCE` timeline events |
| Evidence export | `src/ailuros/evidence/export.py` | **IMPLEMENTED** | `export_evidence()`, `export_evidence_json()`, `export_evidence_jsonl()` |
| Evidence CLI | `src/ailuros/cli.py` (evidence subcommand) | **IMPLEMENTED** | `evidence <run_id>` with `--output json|jsonl`; tested in `tests/test_evidence_export_cli.py` |
| Evidence evaluation | `src/ailuros/evaluation/` | **IMPLEMENTED** | Evidence-based evaluation service and cases; tested in `tests/test_evidence_evaluation.py` |
| Evidence regression | `src/ailuros/regression/` | **IMPLEMENTED** | Evidence-based regression service and timeline; tested in `tests/test_evidence_regression.py` |
| Pipeline boundary guard | `tests/test_evidence_pipeline_boundary.py` | **IMPLEMENTED** | Guards against domain-specific reference-app terms in `src/ailuros/` and server write methods |
| Core boundary guard | `tests/test_core_boundary.py` | **IMPLEMENTED** | Broader boundary: no `clarify`, `browser`, `sidepanel`, `cta` in core; server is read-only |
| Audit package evidence section | `src/ailuros/audit/package_export.py` | **IMPLEMENTED** | Audit packages include `evidence` key with evidence events; tested in `tests/test_evidence_audit_package.py` |
| Evidence demo | `examples/evidence_demo.py` | **IMPLEMENTED** | `run_demo()` exercises full pipeline: ingest → export → evaluate → regress; tested in `tests/test_evidence_demo.py` |
| Storage: no dedicated evidence table | `tests/test_evidence_storage_gap.py` | **IMPLEMENTED** | Evidence stored via generic `events` table; no dedicated `append_evidence()` method needed |
| Evidence record contract | `tests/test_evidence_record_contract.py` | **IMPLEMENTED** | Field validation, serialization, extra-field rejection, tz enforcement, domain-neutral event_type |
| Reference-app fixtures | `examples/reference_apps/fixtures/` | **IMPLEMENTED** | Clarify, EverRun, RadarCreation JSON fixtures exist and validate against evidence contract |
| Server write API | — | **NON-GOAL** | No `do_POST`/`do_PUT`/`do_PATCH`/`do_DELETE` in `src/ailuros/server/` |
| Real Clarify/browser/radarCreation integration | — | **NON-GOAL** | Domain-specific vocabulary never enters `src/ailuros/` core |
| Multi-agent orchestration | — | **NON-GOAL** | Single-process local runtime only |
| Dashboard / UI / observability frontend | — | **NON-GOAL** | No UI platform in scope |
| Phase 5 platformization | — | **NON-GOAL** | No multi-tenant server, REST API, Postgres, MCP, adapter ecosystem |

## Required checks

- All items in the Acceptance Matrix marked **IMPLEMENTED** must have their source
  files, test files, and validation commands present and passing.
- Release smoke script (`scripts/check_release_v020.py`) passes.
- Full test suite passes.
- No `src/ailuros/` source file is modified by this acceptance gate.

## Non-goals

- This release does not introduce an HTTP write API or server write methods
  (`do_POST`, `do_PUT`, `do_PATCH`, `do_DELETE`).
- This release does not introduce a reference-application core dependency;
  Clarify vocabulary does not enter `src/ailuros/`.
- This release does not introduce automatic evidence-only review mode.
- This release does not introduce multi-agent orchestration or automatic remediation.
- This release does not introduce dashboard, UI platform, or observability frontend.
- This release does not introduce Phase 5 platformization features (multi-tenant
  server, REST API, Postgres, MCP, adapter ecosystem).
- This release does not claim server write API support.
- This release does not claim real Clarify integration inside core.

## Explicit deferrals (Phase 1 boundary)

| Feature | Rationale |
|---|---|
| HTTP write API | Ailuros remains in-process; v0.2 uses local ingestion |
| Auth / session management | Out of scope for local runtime kernel |
| Dashboard or UI | Not a platformization goal |
| Realtime browser blocking | No runtime control from browser into Ailuros |
| Clarify-defined core schema | Clarify-specific types never appear in `src/ailuros/` |
| Full Clarify browser adapter | Adapter contract exists; no browser adapter is implemented |
| Phase 5 platformization | Multi-tenant server, REST API, Postgres, MCP |

## Validation commands

Run:

```
python scripts/check_release_v020.py
python -m pytest tests/test_release_v020.py -q
python -m pytest tests -q
```
