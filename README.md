# Ailuros Runtime Governance

Python-first runtime governance SDK for local agent experiments.

## Quickstart

```bash
python -m pip install -e ".[dev]"
python -m ailuros --help
python examples/refund_agent/main.py
python -m ailuros run list
```
